services:
  - type: web
    name: Predictify FastAPI app
    runtime: python
    buildCommand: "pip install -r requirements.txt && python models/train_model.py"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0